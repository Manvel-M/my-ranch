<script>
  import { observer } from "@/utils/observer";
  const observe = observer({
    threshold: 0.4,
  });

  document.addEventListener("DOMContentLoaded", () => {
    // document.querySelectorAll<HTMLElement>("[data-inview]").
    // forEach((el, idx) => {
    //   setTimeout(() => observe.observe(el), (idx + 1) * 100);
    // });

    const element = document.querySelectorAll<HTMLElement>("[data-inview]");
    element.forEach((el) => {
      observe.observe(el);
    });
  });
</script>
